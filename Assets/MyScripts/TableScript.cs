using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TableScript : MonoBehaviour
{
    // Start is called before the first frame update

    void OnEnable()
    {
        //GameObject holderObject = GameObject.FindGameObjectWithTag("cueHolder");
        //Physics.IgnoreCollision(holderObject.GetComponent<Collider>(), GetComponent<Collider>());
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
